<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-8 offset-md-1'>
      <h2>グループ一覧</h2>
      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th>グループ名</th>
            <th>紹介文</th>
            <th>メンバー数</th>
            <th>ステータス</th>
          </tr>
        </thead>
        <tbody>
          <% @groups.each do |group| %>
            <tr>
              <td><%= group.name %></td>
              <td><%= group.introduction %></td>
              <td><%= group.group_users.count %></td>
              <% if group.owner_id == current_user.id && group.permits.exists? %>
                <td>オーナー：承認待ちユーザーがいます</td>
              <% elsif group.owner_id == current_user.id %>
                <td>オーナー</td>
              <% end %>
              <% if (group.owner_id != current_user.id) && (group.group_users.exists?(user_id: current_user.id)) %>
                <td>加入中</td>
              <% end %>
              <% if (group.owner_id != current_user.id) && (group.permits.exists?(user_id: current_user.id)) %>
                <td>加入許可待ち</td>
              <% end %>
                <td><%= link_to 'グループ詳細', group_path(group), class: "btn btn-outline btn-secondary" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
