<div class="text-center text-xl font-bold mt-10">②メモを要約・保存</div>

<div class="flex flex-col items-center mt-4">
  <%= form_with model: @summary, local: true do |form| %>
    <div class="mb-4 w-full max-w-md">
      <%= form.label :title, 'タイトル'%>
      <%= form.text_area :title, rows: 1, class: "w-full" %>
      <%= form.label :content, 'メモ内容'%>
      <%= form.text_area :content, rows: 5, class: "w-full" %>
    </div>
    <div class="flex justify-between w-full max-w-md" style="margin-top: 1cm;">
      <div class="flex-1 mr-2">
        <button class="btn btn-outline btn-success w-full">
          <%= form.submit "AI要約して保存" %>
        </button>
      </div>
      <div class="flex-1 ml-2">
        <%= link_to summaries_path do %>
          <button class="btn btn-outline btn-secondary w-full">練習メモ一覧へ</button>
        <% end %>
      </div>
    </div>
  <% end %>
  <p>※入力した原文は一覧画面で確認可能です</p>
</div>
