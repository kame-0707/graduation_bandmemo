<%= link_to group_summary_like_path(group_id: @group.id, summary_id: summary.id), id: "unlike-button-for-summary-#{@group.id}-#{summary.id}", data: { turbo_method: :delete }, remote: true do %>
    <i class="fa-solid fa-heart"></i>
<% end %>