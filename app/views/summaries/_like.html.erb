<%= link_to group_summary_like_path(group_id: @group.id, summary_id: summary.id), id: "like-button-for-summary-#{@group.id}-#{summary.id}", data: { turbo_method: :post }, remote: true do %>
    <i class="fa-regular fa-heart"></i>
<% end %>