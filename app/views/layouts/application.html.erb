<!DOCTYPE html>
<html data-theme="nord">
  <head>
    <!-- OGPの設定 -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content="bandmemo" />
    <meta property="og:description" content="bandmemoで、バンド練を振り返ろう。" />
    <meta property="og:url" content="https://bandmemo.onrender.com" />
    <meta property="og:site_name" content="bandmemo" />
    <meta property="og:image" content="https://i.gyazo.com/90d84c2d3f4c954b60df493bbcbf105e.png" />
    <!-- Twitterカードの設定 -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@kamekame_manabi" />

    <title>bandmemo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <div class="font-notosansjp">
      <div class="min-h-screen w-full relative pb-12">
        <div class="w-full">
          <% if logged_in? %> 
            <%= render 'shared/header' %>  
          <% else %> 
            <%= render 'shared/before_login_header' %> 
        <% end %> 
        </div>
        <div class="my-5">
          <!-- フラッシュメッセージの表示 -->
          <%= render 'shared/flash_message' %> 
          <%= yield %>
        </div>
        <div class="absolute bottom-0 w-full">
          <!-- フッター -->
          <%= render 'shared/footer' %>
        </div>
      </div>
    </div>
  </body>
</html>


